<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/button_primary</item>
        <item name="colorPrimaryDark">@color/button_primary_pressed</item>
        <item name="colorAccent">@color/button_primary</item>
        <item name="android:windowBackground">@color/background_color</item>
    </style>

    <!-- Welcome Activity theme (no action bar) -->
    <style name="WelcomeTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/button_primary</item>
        <item name="colorPrimaryDark">@color/button_primary_pressed</item>
        <item name="colorAccent">@color/button_primary</item>
        <item name="android:windowBackground">@color/background_color</item>
        <item name="android:statusBarColor">@color/background_color</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

<!--    rounded corner for image-->
    <style name="RoundedImage" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>

    <!-- Tab text appearance -->
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>